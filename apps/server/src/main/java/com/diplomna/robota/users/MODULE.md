# users

## Purpose

Owns persisted User identity data and the current-user read endpoint.

## Public surface

- `GET /api/users/me`

## Owns

- `users` table
- User persistence model

## Depends on

- Shared base entity fields
